import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="hero-card">
        <p className="eyebrow">Issue 1 of 16</p>
        <h1>Vite + React + TypeScript is ready</h1>
        <p className="description">
          This repository now has a strict TypeScript frontend foundation with
          ESLint and Prettier configured for the remaining issues.
        </p>
      </section>
    </main>
  )
}

export default App
